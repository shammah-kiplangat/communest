# Copy to frontend/.env.local for local development.
# "/api" routes through the Vite dev proxy to the backend on http://localhost:3000.
# Point it at a full URL (e.g. https://communest-backend.vercel.app/api) to hit a
# deployed backend instead.
VITE_API_URL=/api
