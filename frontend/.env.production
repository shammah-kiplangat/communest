# Committed on purpose: this is a public URL, not a secret. It is the default API
# host for every production build. A VITE_API_URL set in the Vercel dashboard
# overrides this file, which is how preview deployments point somewhere else.
VITE_API_URL=https://communest-backend.vercel.app/api
