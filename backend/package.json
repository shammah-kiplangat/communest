{
  "name": "communest-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --include .env --include .env.local dev-server.ts",
    "dev:vercel": "vercel dev --listen 3000",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^3.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "vercel": "^58.11.0"
  }
}
